# Sakar Robot Management Platform

**This is the single project workspace for the Sakar Robotics Robot Management Platform. All new source code, infrastructure, tests, and documentation for this platform must live under this root (`D:\Sakar Robotics Projects\sakar robotics web`).**

## CURRENT PHASE

**PHASE 6 — FLEET OPERATIONS BACKEND AND FRONTEND INTEGRATION COMPLETE (Users/Roles, Tasks, Alerts, Cleaning history, Remote Commands non-lock) (branch `feature/phase6-fleet-operations`, uncommitted — pending review/approval).**

The backend has real, tested REST endpoints for user/role administration, robot task orchestration, alert generation/acknowledgement, cleaning-session history, and non-lock remote commands — see Implementation Status below for exactly which modules moved from foundation-only to implemented, and `docs/requirements/SAKAR_PLATFORM_KEENON_PARITY_REQUIREMENTS.md` for three **proposed, unapproved, not-implemented** product-expansion modules (Open Platform, OTA/Deployment, IoT/Elevator) scoped separately per an explicit decision not to build them from Keenon Cloud reference screenshots alone.

The Sakar Web Application (`web/`, React + TypeScript) now consumes all six of those Phase 6 endpoints for real: Users, Roles, Tasks (including per-robot lifecycle control), Alerts (acknowledge/resolve), Cleaning history, and non-lock robot Commands (a new Robot Detail tab), on top of the pre-existing real wiring for authentication, organizations, sites, robots, and audit logs. `web/src/mocks/simulated.ts` had its `generateAlerts`/`generateTasks`/`generateUsers`/`generateRoles` generators removed entirely (superseded by real API calls) — Telemetry, Events, Errors, Application Logs, and the Robot Detail Timeline tab remain simulated, clearly bannered, because no backend REST endpoint exists for any of them yet. **Full live-browser verification against a real running backend was not performed this pass** (no Docker/Postgres/Redis available in this environment) — verification here is TypeScript compilation, a full production build, the existing 21 frontend tests, 7 new mocked-API render/interaction tests against the real DTO shapes (Users/Alerts), and a careful manual cross-check of every new frontend type/API call against the actual backend controller/DTO source. A prior UI/UX pass restyled the whole application as a professional enterprise fleet-management console with no change to backend logic, RBAC, or authentication. See `docs/architecture/SAKAR_WEB_APPLICATION_ARCHITECTURE.md`, `docs/architecture/SAKAR_WEB_UI_DESIGN_SYSTEM.md`, `docs/requirements/SAKAR_PHASE_4_WEB_IMPLEMENTATION_REPORT.md`, and `docs/requirements/SAKAR_WEB_UI_UX_REQUIREMENTS.md` for the full prior account, including an earlier live browser verification against a real running instance of the backend.

Separately, Phase 3's own MQTT pipeline was validated against a real (locally-hosted) MQTT broker after this README was last written — see `docs/requirements/SAKAR_PHASE_3_LIVE_MQTT_VALIDATION_REPORT.md`: MQTT connection/auth/TLS/ACL, heartbeat, telemetry, dedup, replay, cross-tenant isolation, rate limiting, and reconnect all passed against a real broker with the real, unmodified backend and agent code — still **using a test agent, not a physical CleanBot**, and still uncommitted.

**Also this pass (Phase 7 — "Robot Agent Command Loop", branch `feature/robot-agent-command-loop`, uncommitted):** closes the loop `RobotCommandService` (Phase 6) started but could not finish because `SakarC40Agent` had no command-consuming code. `SakarC40Agent` now subscribes to its own `commands` topic (`CommandDispatcher`), and reports RECEIVED/EXECUTING/COMPLETED/FAILED/TIMEOUT lifecycle transitions back over the *existing* EVENT channel (`eventType="COMMAND_RESULT"` — no new MQTT message type or topic was added). The backend's new `CommandResultIngestionService` applies those reports to `RobotCommand`/`CommandResult` idempotently (a terminal status is frozen; out-of-order reports never regress it), and a new `CommandExpiryService` scheduled sweep resolves commands the agent never answers before `expiresAt`. **This closes the software loop only** — `SakarC40Agent`'s only wired command type, `START_TASK`, is executed by `SimulatedRobotCommandExecutor`, an explicitly-labeled software placeholder, because the officially-distributed Peanut SDK v1.3.0 AAR this agent bundles has **no cleaning-control API at all** (confirmed by direct `javap` decompilation of every class in the AAR: `PeanutSDK`'s complete accessor set is `battery(), device(), disinfect(), door(), elevator(), map(), motor(), navigation(), robot(), runtime(), schedule(), update(), vendor(), deviceNode()` — no `clean()`, and no `com.keenon.sdk.robot.api`/`com.keenon.sdk.coapapi` packages anywhere in the AAR). See `PEANUT_CLEAN_V3.7.6_INTERNAL_OPERATION_ANALYSIS.md` and `PEANUT_SDK_C40_API_MATRIX.md` (both at `D:\Sakar Robotics Projects\`) for the full evidence trail, and the standalone `ROBOT_AGENT_COMMAND_LOOP_INVESTIGATION_AND_DESIGN.md` report for the complete investigation, architecture decision, and physical-validation checklist for this pass. 8 new backend tests (101/101 passing) and 10 new/extended agent `:api` tests (28/28 passing) — **no MQTT broker and no physical robot were used; this is CODE VERIFIED and, for the parts covered by the tests above, SOFTWARE TEST VERIFIED — nothing here is PHYSICAL ROBOT VERIFIED.**

**Second pass, same phase (`RETURN_TO_DOCK`, still on `feature/robot-agent-command-loop`, uncommitted):** extended the command loop with a REAL (non-simulated) executor — `PeanutSdkReturnToDockExecutor` calls the officially-distributed Peanut SDK's `BatteryComponent.autoCharge(IDataCallback, int)` (re-verified this pass by fresh `javap` decompilation; internally `@CoapCommond(path="/charge/auto")`, the identical local endpoint independently found in Peanut Clean's own app), through `C40RobotController.returnToDock()` — gated by the same pre-existing `OperatingMode.HARDWARE_TEST` guard as every other actuation method, never bypassed. Because the SDK callback only confirms the local interface *accepted* the request, not that the robot physically completed it, a new, honest terminal status (`COMMAND_DISPATCHED`, agent-reported as `"DISPATCHED"`) was added to the *existing* lifecycle rather than fabricating `COMMAND_SUCCESS` — no new MQTT topic/message type, no database migration. `START_TASK`'s simulation and `START_CLEANING`'s status are unchanged. 1 new backend test (102/102 passing) and 9 new agent `:api` tests (37/37 passing); `:app` (where the real Peanut SDK wiring lives) compiles successfully against the real local Android SDK. **Still CODE VERIFIED + SOFTWARE TEST VERIFIED only — PHYSICAL ROBOT VALIDATION: NOT PERFORMED.** See `ROBOT_AGENT_COMMAND_LOOP_INVESTIGATION_AND_DESIGN.md` §22.

**Third pass, same phase (`GO_TO_POINT`, still on `feature/robot-agent-command-loop`, uncommitted):** after a dedicated, evidence-only investigation (`C40_S_GO_TO_POINT_SDK_INVESTIGATION.md` — no code changed until it was complete), wired a fourth command type onto the same loop: `PeanutSdkGoToPointExecutor` calls the officially-distributed Peanut SDK's `NavigationComponent.setTarget(IDataCallback, int)` (confirmed via fresh `javap` decompilation; internally `@CoapCommond(path="/navigation/dst", requestType=POST)` — the identical local endpoint independently found actively in use inside Keenon's own C40 S Robot Installation Assistant app), through the **pre-existing** `C40RobotController.goToPoint()` (already present, already `OperatingMode.HARDWARE_TEST`-gated, but previously untested). The `int` is a **pre-registered destination id on the robot's own map, never a raw coordinate and never invented by this project** — both `PeanutSdkGoToPointExecutor` (agent-side) and `RobotCommandService` (backend-side) independently reject a command missing a non-negative integer `destinationId` before the SDK/MQTT publish is ever reached, reusing the exact same validation-failure conventions each layer already had (no new validation framework). Same `DISPATCHED`-not-`COMPLETED` completion-evidence discipline as `RETURN_TO_DOCK`. 4 new backend tests (106/106 passing) and 11 new agent `:api` tests (48/48 passing, including a full `CommandDispatcher → PeanutSdkGoToPointExecutor` integration test with only the SDK boundary faked), plus 2 new `:robot`-module tests for `goToPoint`'s previously-uncovered `OperatingMode` gating (`C40RobotControllerTest`, 6/6 passing); `:app` compiles successfully against the real local Android SDK. **Still CODE VERIFIED + SOFTWARE TEST VERIFIED only — PHYSICAL ROBOT VALIDATION: NOT PERFORMED**, and no destination id has been enumerated or confirmed against any real robot's map. See `C40_S_GO_TO_POINT_SDK_INVESTIGATION.md` and `robot/SakarC40Agent/README.md`.

**Fourth pass, same phase (Destination Discovery, still on `feature/robot-agent-command-loop`, uncommitted):** `GO_TO_POINT` required a caller-supplied `destinationId` with no way to discover one — a dedicated investigation (`C40_S_DESTINATION_DISCOVERY_INVESTIGATION.md`) confirmed the officially-distributed SDK's `NavigationComponent.getAllDestPose(IDataCallback)` supports exactly this (internally `com.keenon.sdk.api.NavigationDestPoseApi`, `@CoapCommond(path="/navigation/dest_poses")`, a plain GET), and wired it end to end: `C40RobotController.getAllDestinations()` → `NavigationBridge` → `PeanutSdkBridge`, returning a new vendor-neutral `Destination` type (`:telemetry`: `Destination`/`Pose`/`Position`/`Orientation`) — **read-only, no `OperatingMode` guard**, code-inspection-confirmed to never call any actuation method. A structurally-different `getAllDestPoseV2()` was investigated and found unsuitable (no pose/map field at all) rather than used by convenience. **No backend endpoint was built**: there is no existing MQTT channel for the agent to report a destination list to the backend, and building one would mean inventing a new, unreviewed reporting mechanism — reported as an explicit limitation rather than built as a hollow stub or a new pipeline. 9 new `:sdk` tests (a pure JSON-parsing function, no SDK singleton touched) and 2 new `:robot` tests, all passing; `:api`/backend unchanged (48/48, 106/106). **CODE VERIFIED + SOFTWARE TEST VERIFIED only — PHYSICAL ROBOT VALIDATION: NOT PERFORMED**; no destination id or name returned by this feature has been confirmed against a real robot. See `C40_S_DESTINATION_DISCOVERY_INVESTIGATION.md`.

**Fifth pass, new Roadmap Phase 9 (Virtual C40 Robot Simulator, still on `feature/robot-agent-command-loop`, uncommitted):** a first physical-installation attempt on this session's own machine found no C40 S connected via `adb` (`adb devices` returned empty — reported honestly, no device/movement command was ever attempted). Rather than block on hardware, built a complete, software-only virtual robot simulator to test the full command architecture first: a new plain-Java `:virtual-agent` Gradle module (`VirtualRobotEngine`/`VirtualRobotRegistry`/`VirtualRobotCommandExecutor`/`VirtualMap`/`VirtualC40AgentRunner`) that reuses the SAME `:api`/`:telemetry` contracts (`CommandDispatcher`, `RobotCommandExecutor`, `AgentMqttClient`, `Destination`, ...) the real agent uses, with **zero Gradle dependency on `:sdk`/`:robot`/`:navigation`/`:charging`** — enforced both by the build graph and by a new automated test (`VirtualAgentRealSdkIsolationTest`) that scans the module's own source for any real-SDK/physical-robot reference. Simulates `GO_TO_POINT`/`RETURN_TO_DOCK` against a deterministic, clearly-fake `VIRTUAL_TEST_MAP` (destinations 1001-1004, explicitly documented as simulation-only), a `START→MOVING→ARRIVED` state machine, deterministic battery drain/charge, offline/blocked/insufficient-battery failure modes, and 3 independent virtual robots — reusing the existing `CommandDispatcher`'s dedup/expiry rather than reimplementing them. No frontend code changed: a new backend test (`VirtualRobotSimulatorIntegrationTest`) proves 3 virtual robots register and accept commands through the EXISTING, unmodified robot-registry/command REST API, using a `"Sakar Virtual Simulator"` manufacturer/model to clearly mark them as simulated. 25 new `:virtual-agent` tests + 1 new backend test (107/107), all passing. **`CODE IMPLEMENTED` + `SOFTWARE TEST VERIFIED` only — `PHYSICAL C40: NOT PERFORMED`.** See `VIRTUAL_C40_SIMULATOR.md`.

**Sixth pass, separate investigation thread (Keenon Cloud API Entitlement Investigation — Phase 1G/1H, same branch `feature/robot-agent-command-loop`, COMMITTED as `fc438df` and pushed to `origin/feature/robot-agent-command-loop`):** live re-testing of the Keenon Open Platform account found `GET /api/open/scene/v1/robot/status` returning `610403 Insufficient operation permission` for this account's C-series (C40 S) robots, despite historical evidence of it working on 2026-08-25. Official-documentation review established why: the endpoint is documented for T-series/W-series robots only, never C-series — the account's authorization enforcement simply started matching its own documented scope, not a regression or a vendor-side bug. `KeenonApiClient.getRobotStatus` now calls the correct C-series endpoint (`/api/open/custom/clean/robot/status`, `robotSn` param) — live-verified `610000` with a full `hardwareState`/`globalState`/`childState`/`mainState`/`subState` payload for the real "Demo Piece" robot (`94:BA:06:CA:99:F3`). A separate, pre-existing URI double-encoding bug in `authorizedGet` (re-encoding an already-percent-encoded query string, turning `%3A` into `%253A`) was fixed in the same file. Battery (`custom/robot/battery/level`) was unaffected by the migration and re-verified live (84%, not charging). Separately (Phase 1H): added a manual/on-demand trigger, `POST /api/v1/robots/{id}/keenon/cleaning-history/sync` (`ROBOT_CONFIGURE`-gated, explicit `storeId` required in the body, never inferred), for the existing scheduler-only, disabled-by-default `KeenonCleaningHistorySyncService` — live-verified with exactly one real sync call against Demo Piece (`0` new records — no duplicates, no vendor error, existing history unchanged). Both changes are live-verified against the real Keenon account (not simulated), matching the same evidence discipline as the Peanut SDK passes above. 7 new/updated backend test files across `KeenonApiClientTest` (new), `KeenonRobotAdapterTest` (extended), and `KeenonCleaningHistorySyncControllerTest` (new); no source files outside `integration/keenon/` touched. **Investigation continues, not yet implemented:** a robot's current scene/map identity can no longer be resolved via the old status endpoint's `sceneCode` field (absent from the C-series response) — `KeenonMapMetadataSyncService`'s existing reliance on that field is a known, now-documented gap; `custom/clean/robot/area/list`'s `mapId` field is the only unproven candidate found so far (structurally a different identifier format from `sceneCode` — not assumed equivalent). No code has been changed for this open question. **Documentation debt found, not fixed by this pass:** the full backend suite is actually **364 tests, all passing** as of this commit — well above the `107/107` recorded in the Fifth pass above. A substantial amount of already-existing Keenon integration work (area/cleaning-mode/back-point/map-metadata/map-image/map-point sync, cleaning history, and more) predates this pass, is not part of the Phase 7 agent-command-loop story, and was never folded into this CURRENT PHASE narrative or the Implementation Status table below. That reconciliation is out of scope for this pass and remains outstanding.

**Seventh pass, same investigation thread (Keenon C40 S Map/Scene Integration — Phase 1I, same branch `feature/robot-agent-command-loop`, COMMITTED and pushed to `origin/feature/robot-agent-command-loop` — see `git log -1 --oneline` for the exact hash):** resolved the Sixth pass's open question. Live testing proved the C-series `custom/clean/robot/status` response has no `sceneCode`/`sceneName`/`mapId` field at all (a real captured payload is pinned in `KeenonRobotAdapterTest`), and the only endpoint that ever returned `sceneCode` (`scene/v1/robot/status`) is rejected `610403` for this account on C-series robots; `custom/clean/robot/area/list`'s `mapId` was live-confirmed a differently-shaped identifier, never accepted as `sceneCode` by the map endpoints. A robot's `sceneCode` is therefore now Sakar-owned, per-robot configuration data — a new `KeenonRobotSceneConfig` entity/table (`keenon_robot_scene_configs`, migration `V16`), set via `PUT /api/v1/robots/{id}/keenon/scene-config` (`ROBOT_CONFIGURE`-gated) and read via `GET` on the same path — never a global default, never inferred from another robot or a historical scene. `KeenonMapMetadataSyncService` now resolves `sceneCode`/`sceneName` from that config instead of any vendor call (it no longer depends on `KeenonApiClient` at all); `KeenonMapImageSyncService`/`KeenonMapPointSyncService` are unchanged, since they already only depend on `KeenonMapMetadataSyncService`'s output. A new manual/on-demand trigger, `POST /api/v1/robots/{id}/keenon/map/sync` (mirroring the existing area/cleaning-history sync pattern), was added since previously only the disabled-by-default scheduler could sync map metadata/image at all. Live-verified for Demo Piece (`94:BA:06:CA:99:F3`): current scene `7ClJPR`/`F`, map endpoints both `610000`, `mapMd5 a3cb0d75faa17c9ab12b9a6434173b42`, image `570×763`. The robot's scene was manually switched by the operator from the historical `SR Cleaning`/`dTW2N7` to `F`/`7ClJPR` between the Sixth and Seventh passes — this is an operator action, not evidence of Keenon or the robot automatically reassigning scenes; the architectural conclusion (never hardcode `sceneCode`, never fall back to a historical value) holds regardless. See `docs/KEENON_C40S_MAP_SCENE_INTEGRATION.md` for the full evidence trail. 25 new/updated backend tests; full backend suite **373/373 passing** (clean `./mvnw clean test`). Frontend: 54/54 tests passing, unaffected by this backend-only pass — `npx tsc --noEmit`/`npm run build` fail with 2 pre-existing errors confined entirely to the unrelated, untracked, not-part-of-this-commit `web/src/features/robots/useRobotMapImage.ts` (orphaned since an earlier, unrelated data-loss incident — see git history).

**Eighth pass, Frontend Map Image Slice 1 (same branch `feature/robot-agent-command-loop`):** wired `RobotMapPanel` to the backend's authenticated map endpoints, closing the exact gap the orphaned `useRobotMapImage.ts` hook (above) was left mid-attempt on. That hook's own logic was already correct (loading/success/not-found/error states, object-URL create-then-revoke lifecycle with no leak across refetch/unmount) — its only real defect was that `getRobotMapImage` didn't exist yet in `api/robots.ts`. Added `getRobotMapImage` (`GET /robots/{id}/map/image`, fetched as a `Blob` via `apiClient`'s `responseType: 'blob'` — not the `ApiResponse<T>` JSON envelope every other endpoint uses, since the backend responds with raw PNG bytes) and `getRobotMap` (`GET /robots/{id}/map`, the existing metadata endpoint, now consumed by the frontend for the first time) plus a matching `RobotMapMetadata` type. `RobotMapPanel`'s Map card now renders the actual stored PNG (built from a local `blob:` object URL — the browser can't attach the JWT to a plain `<img src>`, and the vendor/Keenon URL and any backend filesystem path are never exposed to the client either way) alongside the map's name/dimensions from `getRobotMap`, with the image, metadata, and areas each fetched independently so one failing never hides what the others returned. Map points, area-polygon overlays, live robot position, and zoom/pan are explicitly **not** part of this slice — the panel says so plainly rather than faking any of it. 11 new/updated frontend tests (loading/success/404/generic-error/retry/object-URL-cleanup-on-refetch/object-URL-cleanup-on-unmount for the image, plus the pre-existing areas coverage adapted to the new independent-sections layout); one pre-existing `RobotDetailPage.test.tsx` case updated to mock the now-real `getRobotMapImage` call instead of asserting removed placeholder text. **`npx tsc --noEmit`: 0 errors** (the 2 errors this file caused above are gone). Frontend suite: **59/59 passing**. `npm run build`: **BUILD SUCCESSFUL**. No backend code changed for this pass.

**Ninth pass, on `dev` after PR #15 merged `feature/robot-agent-command-loop`'s full history in (Command Result History API):** completed a previously-orphaned, untracked DTO (`command/dto/CommandResultResponse.java` — present in the working tree but never committed, with zero call sites) into a real, authenticated read endpoint: `GET /api/v1/robots/{robotId}/commands/{commandId}/results`, added to the existing `RobotCommandController`/`RobotCommandService` (no new controller/service class). Returns the `command_results` append-only lifecycle ledger for one command, newest-first, paginated with the same `page`/`pageSize` convention as the sibling `GET /commands` list endpoint. Authorization reuses two existing tenant-safe resolvers unchanged (`RobotService#getAccessibleOrThrow` for the robot, `RobotCommandService#getAccessibleOrThrow` for the command) plus one new check neither already performed — that the resolved command actually belongs to the resolved robot, reported as the same `COMMAND_NOT_FOUND` as an unknown command (never a distinguishing error, matching this codebase's existing anti-enumeration posture). One new repository query (`CommandResultRepository.findByCommandIdOrderByCreatedAtDesc`, reusing the existing indexed `command_id` column — no migration needed). 7 new tests on `RobotCommandControllerTest` (history ordering/field-mapping, pagination, empty history, unknown command, command belonging to another robot, cross-organization robot, missing `ROBOT_VIEW` permission). Full backend suite: **380/380 passing** (`./mvnw clean test`), `BUILD SUCCESS`. No frontend/Keenon/agent code touched; no existing command dispatch/execution semantics changed.

**Tenth pass, Command Lifecycle History Panel (branch `feature/map-point-overlay`, built off `dev` at `c562023`, not yet committed):** consumed the Ninth pass's Command Result History API from the frontend for the first time. `CommandsPanel`'s existing "Command History" table gained a "View history" action per row; selecting it renders a new "Command Lifecycle History" card showing that command's full `command_results` ledger — `result`, `detail`, `durationMs` (or `—` when absent), and `createdAt` — newest-first, exactly matching the backend's own ordering (no client-side re-sort). Added `getCommandResults(robotId, commandId, page, pageSize)` to `api/commands.ts`, mirroring `listCommands`'s existing request/response/error conventions exactly, and a `CommandResult` type in `types/domain.ts` mirroring the backend `CommandResultResponse` DTO field-for-field. Loading/populated/empty (`"No lifecycle events recorded yet"`)/error states all handled via the same `useApi`/`LoadingState`/`ErrorState`/`EmptyState` conventions used everywhere else in this codebase — no new UI library, no redesign of the existing Commands tab. 8 new tests on a new `CommandsPanel.test.tsx` (existing list unchanged, history hidden until selected, correct endpoint/params, loading, populated newest-first with full field mapping, empty, error+retry, hide-history toggle). **`npx tsc --noEmit`: 0 errors. Frontend suite: 67/67 passing. `npm run build`: BUILD SUCCESSFUL.** No backend/database/Keenon/agent code touched; Map Slice 2 (Charging Point + Starting Point Overlay) remains **BLOCKED** pending Keenon support's confirmation of the `positionX`/`positionY` → PNG pixel coordinate transformation — see the escalation message drafted for that investigation. (COMMITTED as `d1862df` and pushed to `origin/feature/map-point-overlay` — since committed, despite an earlier revision of this row saying otherwise.)

**Eleventh pass, Keenon Scene Configuration Panel (branch `feature/map-point-overlay`, off `dev` at `c562023`, not yet committed):** activated the Robot Detail "Configuration" tab (previously a bare `NotConnectedTab` placeholder) with a real UI for the Ninth-pass-adjacent `GET`/`PUT /robots/{id}/keenon/scene-config` endpoints (`KeenonSceneConfigController`, already implemented/merged) — closing the last gap in the Keenon map story: without this, onboarding any new KEENON_CLOUD robot's `sceneCode` required a developer to run a raw authenticated `curl`/Postman call directly against the backend. New `KeenonSceneConfigPanel.tsx` shows the configured `sceneCode`/`sceneName` (or a clear "Not configured yet" state with a setup form, or "Not available for this robot" for a non-`KEENON_CLOUD` model, driven by the existing 404/422 distinction the backend already returns) and, for a caller with `ROBOT_CONFIGURE`, a form to set/update it (`sceneCode` required and trimmed, `sceneName` optional and trimmed to `null` when blank) — a successful save uses the existing toast convention and refetches the displayed configuration. Added `api/keenon.ts` (`getSceneConfig`/`setSceneConfig`, mirroring every other API file's conventions exactly) and a `SceneConfig` type mirroring the backend `SceneConfigResponse` DTO field-for-field. No new UI library, no redesign of Robot Detail — only the `configuration` tab's render branch changed. 10 new tests on a new `KeenonSceneConfigPanel.test.tsx` (correct GET call, loading, configured state with/without `sceneName`, 404 setup state, 422 unavailable state, generic error+retry, blank-sceneCode validation, successful PUT with trim + refetch + toast, form hidden without `ROBOT_CONFIGURE`) plus one new `RobotDetailPage.test.tsx` case proving the Configuration tab renders the real panel instead of the placeholder. **`npx tsc --noEmit`: 0 errors. Frontend suite: 78/78 passing. `npm run build`: BUILD SUCCESSFUL.** No backend/database code touched — both endpoints were already implemented and tested. Map Slice 2 remains **BLOCKED** pending Keenon support's confirmation of the `positionX`/`positionY` → PNG pixel coordinate transformation.

**Twelfth pass, Connect CLEANING RobotTask START to the existing RobotCommand pipeline (branch `feature/map-point-overlay`, not yet committed):** a live-log investigation found that clicking "Start" on a `CLEANING` task returned `200 OK` and updated `robot_tasks`/`task_events`/`audit_logs`, but **never created a `robot_commands` row and never issued a real Keenon HTTP call** — confirmed live: `robot_commands` had exactly **zero rows in the entire database** for any robot. Root cause: `RobotTaskService`/`RobotTaskController` were always Sakar-side bookkeeping only, by explicit prior design (see the Task orchestration row below, historically) — a `CLEANING` task's `START` transition never called into `RobotCommandController`/`RobotCommandService` at all; that pipeline already existed and worked (`KeenonRobotAdapter` → `KeenonApiClient`), it was just never reached from the Tasks UI. Fixed by bridging `RobotTaskService.transition("START")`, for `CLEANING` tasks only, into the **existing** `RobotCommandService.issue(principal, robotId, "START_TASK", params)` — the exact same internal service method `RobotCommandController` itself calls — reusing its command-type/mode/area validation and Keenon-adapter dispatch wholesale; no second Keenon integration, no second OAuth client, and `RobotTaskService` never calls `KeenonApiClient` directly. A `CLEANING` task's `parameters` column must now be a JSON object with `mode` (one of the Keenon-supported cleaning modes) and a non-empty `areaIds` list (Sakar `KeenonAreaMapping` row ids, from `GET /robots/{id}/areas`); `repeatCount` is optional and defaults to `1`. Missing/malformed parameters are rejected `400 VALIDATION_FAILED` before any task-status change or command is ever attempted (the task stays `CREATED`, safely retryable). If the command is created but not dispatched (rejected before or by the vendor, an adapter/OAuth failure), the task is moved to the **pre-existing, previously-unused** `FAILED` lifecycle status (already modeled in `TaskLifecycleStatus`/`TaskStatus` and the frontend's status-badge map, just never produced by anything until now) with a `START_FAILED` `TaskEvent` and a `TASK_START`/`FAILED` audit row recording the reason — mirroring the same "a normal 200 response whose payload honestly reports the outcome" convention `POST /robots/{id}/commands` already uses, rather than inventing a new error path. The task only ever reaches `RUNNING` when the command was genuinely dispatched. On the frontend, the stale `RobotArea` type (missing the backend's own `sakarAreaId` field, even though `GET /robots/{id}/areas` already returned it) was corrected, and `RobotTasksPanel`'s Create Task form now collects a cleaning mode, a real multi-select of synced areas (never hardcoded, never invented), and an optional repeat count for `CLEANING` tasks, submitting a real structured JSON `parameters` payload instead of `null`. **Reported gap, not silently worked around:** no backend endpoint currently lists the set of Keenon-supported cleaning modes (`SWEEP`/`SWEEP_MOP`/`SWEEP_VACUUM`/`SWEEP_PUSH`/`WATER_SUCTION` exist only as a literal `switch` inside `KeenonRobotAdapter.cleanModelIdFor`) — the dropdown mirrors that exact literal set (the same "hardcode the backend's own enum client-side" convention `CommandsPanel.tsx` already uses for `NonLockCommandType`), documented here rather than presented as if sourced from a live API; a real "list cleaning modes" endpoint remains a follow-up. 10 new backend tests (`RobotTaskServiceTest`, new) + 3 new/1 rewritten backend tests (`RobotTaskControllerTest`) covering the dispatch bridge, structural validation, the `FAILED` outcome, and proving non-`CLEANING`/non-`START` transitions are completely untouched; full backend suite **403/403 passing**, `BUILD SUCCESS`. 10 new frontend tests (`RobotTasksPanel.test.tsx`, new) plus 2 existing-test fixture updates (`RobotMapPanel.test.tsx`/`RobotDetailPage.test.tsx`, for the corrected `RobotArea` type); full frontend suite **88/88 passing**, `npx tsc --noEmit`: 0 errors, `npm run build`: BUILD SUCCESSFUL. **Physical robot validation: NOT PERFORMED** — this closes the software/API gap between the Tasks UI and the already-existing, already-live-tested Keenon dispatch pipeline; no live `curl`/browser test against a real running backend+Keenon account was performed for this pass, and no physical C40 has executed a cleaning task as a result of this change.

**Thirteenth pass, corrective slice — duplicate-dispatch protection + malformed areaIds validation (branch `feature/map-point-overlay`, not yet committed):** a read-only code review of the Twelfth pass found two issues before it could be considered safe. **C1 (critical):** `RobotTaskService.transition("START")` read the task via a plain, unlocked `findById` and only persisted the new status *after* the real Keenon dispatch attempt — two genuinely concurrent `/start` requests for the same task (a double-click, two browser tabs, a client retry) could both observe `CREATED` and both dispatch `START_TASK` to the physical robot before either transaction committed. Fixed with a **targeted pessimistic row lock**, not `@Version`/optimistic locking added application-wide: a new `RobotTaskRepository.lockByIdForUpdate(UUID)` (`@Lock(LockModeType.PESSIMISTIC_WRITE)`, `SELECT ... FOR UPDATE`) is used *only* for the `START` transition (every other read — GET endpoints, `PAUSE`/`RESUME`/`STOP`/`CANCEL` — is completely unchanged, still the plain unlocked `findById`). A second concurrent `START` for the same task now blocks at the real database level until the first transaction commits, then re-reads the now-`RUNNING`/`FAILED` status and is correctly rejected by the pre-existing `ALLOWED_FROM` check (`409 INVALID_TASK_TRANSITION`) — it can never reach the dispatch code a second time. Proven with a genuine two-thread integration test (`RobotTaskControllerTest.concurrentStart_...`, using `CountDownLatch`es to force real lock contention through the actual H2 database and a slow-mocked `RobotCommandService.issue()`) plus deterministic single-threaded regression tests (`RobotTaskServiceTest`) confirming the locked repository method is used only for `START` and that a second `START` after the first has already transitioned never re-dispatches. **M1 (medium):** `KeenonRobotAdapter.startTask`'s `UUID.fromString(...)` call on each `areaIds` entry throws an uncaught `IllegalArgumentException` for a malformed (non-UUID) string — not caught by `RobotCommandService`'s `catch (ApiException ex)` — which previously could surface as an unhandled `500` and roll back the just-created `RobotCommand` row. Fixed with one additional structural check inside `RobotTaskService.parseCleaningStartParams` — each `areaIds` entry is now validated as a syntactically well-formed UUID (`java.util.UUID.fromString`, caught) *before* `RobotCommandService.issue()` is ever called, rejected as `400 VALIDATION_FAILED` with the task left untouched at `CREATED`. This is a structural check only — whether an id resolves to an *active* `KeenonAreaMapping` remains `KeenonRobotAdapter`'s own, unduplicated, responsibility. No Keenon OAuth/API/adapter architecture, `RobotCommandController`, `RobotCommandService`'s existing direct-Commands-API behavior, JWT/RBAC, or database schema were touched — only `RobotTaskRepository`/`RobotTaskService` and their tests. 3 new/13 total tests in `RobotTaskServiceTest` and 1 new/9 total tests in `RobotTaskControllerTest` (the new concurrency test run repeatedly, no flake observed); full backend suite **407/407 passing**, `BUILD SUCCESS`, `./mvnw -DskipTests package` also succeeds. Frontend unchanged (no production frontend code needed for either fix) — **88/88 passing**, `npx tsc --noEmit`: 0 errors, `npm run build`: BUILD SUCCESSFUL. **Physical robot validation: still NOT PERFORMED** — both fixes are software/database-level correctness fixes, not physical-robot-dependent.

**Fourteenth pass, Keenon cleaning-area-list response parsing fix (branch `feature/map-point-overlay`, not yet committed):** closes the root cause behind the CLEANING task form's "Could not load areas: No synced Keenon store mapping" blocker. A live, raw (unparsed) capture of `GET /api/open/custom/clean/robot/area/list` for the real account/store/robot revealed the true response envelope — `{code, msg, data: {count, currentPage, pageSize, entities: [...]}}` — where each `entities` element is a per-map/floor GROUP carrying `mapId`/`floor` plus two PARALLEL arrays, `areaIdList`/`areaNameList` (paired by index), never a flat list of `{areaId, areaName}` objects. Two earlier assumptions about this shape were both wrong and both silently produced zero areas instead of failing loudly: the original code read a flat array directly under `data`; an intermediate fix (based on this endpoint's sibling `clean/log/list` envelope) read a top-level `entities` array with no `data` wrapper. Neither matches this endpoint's real shape. Fixed with a new shared `KeenonAreaListParser` (used identically by `KeenonRobotAdapter.getAreas` and `KeenonAreaSyncService.sync`, avoiding duplicated flattening logic) that reads `data.entities[]` and pairs `areaIdList[i]`/`areaNameList[i]` strictly by index — a real vendor length mismatch drops the unpaired trailing entry rather than guessing a pairing. `mapId` is preserved via the pre-existing `keenon_map_id` column; `floor` is parsed (available for future use) but not persisted — no existing column represents it and nothing downstream needs it today, so no schema change was made for it. 27 new/updated tests across `KeenonAreaSyncServiceTest` (21), `KeenonRobotAdapterTest` (33), and `KeenonAreaSyncControllerTest` (7), including explicit regressions proving neither of the two previously-wrong shapes is silently accepted again; full backend suite **425/425 passing**, `BUILD SUCCESS`, `./mvnw -DskipTests package` succeeds. **Live-verified end to end** against the real Keenon account: `POST .../keenon/areas/sync` with `storeId=C00715655` now returns the real area **"Area5"** (vendor id `8a7bd155598342d08158d34d5a07007d`, map `4c0075859805496eb452187b3cd91107`), a real `keenon_area_mappings` row was created, `GET /robots/{id}/areas` now returns `200` with that area, and the CLEANING task form's Areas selector now shows it as a selectable checkbox — the original blocker is resolved. No frontend/schema/OAuth/RBAC code touched. No cleaning task was created or started as part of this verification.

## Project Status

| Field | Value |
|---|---|
| Current phase | Phase 7/8 — Robot Agent Command Loop (`SakarC40Agent` command consumption + result reporting for `START_TASK`/`RETURN_TO_DOCK`/`GO_TO_POINT`) implemented on top of Phase 6, uncommitted |
| Overall status | Backend + Agent MQTT pipeline implemented, live-broker-validated, test-verified; Phase 6 approved-scope backend work implemented and test-verified; Phase 7/8 wires three command types onto the agent-side command loop (`START_TASK` remains a software simulation — no Peanut SDK cleaning API exists; `RETURN_TO_DOCK` and `GO_TO_POINT` are real, verified SDK calls, both `OperatingMode.HARDWARE_TEST`-gated — see above); Web Application now consumes every Phase 6 endpoint for real (Users/Roles/Tasks/Alerts/Cleaning/Commands), plus the pre-existing real auth/org/site/robot/audit wiring; three product-expansion modules (Open Platform/OTA/IoT-Elevator) remain requirements-only, not implemented; not yet committed/merged; mobile, physical robot validation, and live-browser verification of the frontend pass not performed (no local Postgres/Redis/Docker available) |
| Last updated | 2026-09-07 (Fourteenth pass — Keenon cleaning-area-list response parsing fix, branch `feature/map-point-overlay`) |
| Latest branch | `feature/map-point-overlay`, branched off `dev` at `c562023`. The Tenth pass (`d1862df`) is committed and pushed; the Eleventh through Fourteenth passes' work is **not yet committed** as of this README update — see `git status`/`git diff` before trusting "current" claims |
| Latest commit | `d1862df` — "feat: add command lifecycle history panel" (Tenth pass) is the latest actual commit on this branch; the Eleventh through Fourteenth passes sit uncommitted on top. Run `git log -1 --oneline` for the true current HEAD on whichever branch is checked out. The Phase 7/8 Robot Agent Command Loop narrative below describes work from the now-merged-and-deleted `feature/robot-agent-command-loop` branch; its commits are all in `dev`'s history — see `git log` for the full history |
| Test status | **425 / 425 automated backend tests passing** (`cd backend && ./mvnw clean test`) — up from `380/380`; includes the Twelfth/Thirteenth-pass `RobotTaskServiceTest`/`RobotTaskControllerTest` additions for the CLEANING START → RobotCommandService dispatch bridge and its concurrency/validation hardening, PLUS the Fourteenth-pass rewrite of `KeenonAreaSyncServiceTest` (21 tests), `KeenonRobotAdapterTest` (33 tests), and `KeenonAreaSyncControllerTest` (7 tests) for the corrected `data.entities[]` area-list parsing, plus the pre-existing Keenon integration suite and Ninth-pass Command Result History API tests. **88 / 88 automated frontend tests passing** (`cd web && npm run test -- --run`) — up from `78/78`; includes the Twelfth-pass `RobotTasksPanel.test.tsx` (10 tests) for the CLEANING create-task form; unchanged by the Thirteenth/Fourteenth passes (both backend-only fixes). **48 / 48 automated Robot Agent `:api`-module tests passing** (`cd robot/SakarC40Agent && ./gradlew :api:test`) — unchanged by this pass. **8 / 8 automated Robot Agent `:robot`-module tests passing** (`./gradlew :robot:testDebugUnitTest`). **9 / 9 automated Robot Agent `:sdk`-module tests passing** (`./gradlew :sdk:testDebugUnitTest`). **25 / 25 automated Robot Agent `:virtual-agent`-module tests passing** (`./gradlew :virtual-agent:test`) — `VirtualRobotEngineTest` (13), `VirtualRobotRegistryTest` (4), `VirtualRobotCommandExecutorTest` (6), `VirtualAgentRealSdkIsolationTest` (2) |
| Build status | Backend: **`./mvnw -DskipTests package` — BUILD SUCCESS** (this pass). Frontend: **`npx tsc --noEmit` — 0 errors; `npm run build` — BUILD SUCCESSFUL** (this pass). Robot Agent: **`:api:test`, `:sdk:testDebugUnitTest`, `:robot:testDebugUnitTest`, `:virtual-agent:test`, and `:app:assembleDebug` all BUILD SUCCESSFUL** (unchanged this pass; the `:app` module was compiled against the real local Android SDK; `:virtual-agent` is a plain-Java module with no Android dependency at all, see `VIRTUAL_C40_SIMULATOR.md`) |
| Secrets scan | Clean — no client secrets, access tokens, passwords, API keys, or private keys found in source; no new secret-bearing config was introduced this pass |

This table reflects the actual repository state as of the commands above being run, not a copy-forward of an earlier report — re-run `git branch --show-current`, `git status --short`, `./mvnw clean test`, and `./gradlew :api:test` before trusting it if time has passed.

## Project Purpose

Sakar Robotics is building a platform so that Sakar — not the robot vendor (Keenon) — is the primary system of record for robot data, telemetry, history, authentication, authorization, commands, lock/unlock, users, configuration, analytics, logs, alerts, and fleet management, starting with the first product, **Sakar CleanBot 5000 Plus**, built on the Keenon C40 / C40 S hardware platform. The full rationale, architecture, and security model are specified in `docs/requirements/SAKAR_ROBOT_PLATFORM_MASTER_REQUIREMENTS.md` (the authoritative requirements reference this README is checked against). See [`docs/architecture/SAKAR_ROBOT_PLATFORM_NAMING_AND_MODEL_STRATEGY.md`](docs/architecture/SAKAR_ROBOT_PLATFORM_NAMING_AND_MODEL_STRATEGY.md) for the full product/platform/agent naming strategy.

## Robot Integration Status

**Path 1 — current, live-tested, Keenon-dependent:**
```
Sakar Backend  →  Keenon Robot Adapter  →  Keenon Cloud (https://cloud.robotkeenon.com)  →  Robot
```
The `KeenonRobotAdapter` (`backend/src/main/java/com/sakarrobotics/cloud/integration/keenon/`) implements the live-tested read/control operations (status, battery, telemetry, areas, temporary task, pause, recharge) against Keenon's Open Platform, gated by the robot capability model so unsupported operations return `UNSUPPORTED_CAPABILITY` rather than being attempted. This path is **KEENON-CLOUD DEPENDENT** by design and is explicitly not the target architecture — see Master Requirements Part 10/40.

**Path 2 — target architecture, MQTT telemetry now implemented in software (Phase 3):**
```
SakarC40Agent  →  MQTT  →  Sakar Cloud  →  PostgreSQL  →  WebSocket
```
Presence/heartbeat/telemetry/events/errors flow agent → cloud → database over MQTT, with idempotent ingestion, robot/tenant identity verification, and real-time WebSocket publish — see `docs/architecture/SAKAR_MQTT_ARCHITECTURE.md`. **This is a software pipeline verified by automated tests only — no MQTT broker or physical robot was run against it in this session.** The `SakarRobotAdapter` (REST/adapter-based control path, distinct from this MQTT telemetry path) is still a stub — every method throws `FEATURE_NOT_YET_IMPLEMENTED`; lock/unlock is not implemented on either path.

**Phase 7 addition — non-lock command dispatch now flows both ways in software:** `SakarC40Agent` consumes its `commands` topic (`CommandDispatcher`) and reports RECEIVED/EXECUTING/COMPLETED/FAILED/TIMEOUT back over the EVENT channel; the backend's `CommandResultIngestionService` applies these to `RobotCommand`/`CommandResult`. `START_TASK` is executed by an explicitly-labeled software simulation (`SimulatedRobotCommandExecutor`) — the officially-distributed Peanut SDK AAR this agent bundles has no cleaning-control API to call for it (confirmed by decompilation — see the Current Phase note above and `ROBOT_AGENT_COMMAND_LOOP_INVESTIGATION_AND_DESIGN.md`). A `COMMAND_SUCCESS` status from this pipeline today means "the agent's software simulation finished," never "a robot cleaned anything." **`RETURN_TO_DOCK`, added in a second pass, is executed for real** — `PeanutSdkReturnToDockExecutor` calls the officially-distributed SDK's `BatteryComponent.autoCharge()`, gated by the same `OperatingMode.HARDWARE_TEST` guard as every other actuation method — but its terminal status is `COMMAND_DISPATCHED`, not `COMMAND_SUCCESS`, because SDK acceptance is not confirmed physical completion. **`GO_TO_POINT`, added in a third pass, is likewise executed for real** — `PeanutSdkGoToPointExecutor` calls the officially-distributed SDK's `NavigationComponent.setTarget(IDataCallback, int)` with a caller-supplied, pre-registered destination id (never invented by this project — both the agent and the backend reject a missing/negative one before the SDK or MQTT is touched), gated the same way, also reporting `COMMAND_DISPATCHED` rather than `COMMAND_SUCCESS`.

- Physical C40 validation: **NOT PERFORMED.** No physical robot has been connected to, or controlled by, any code in this repository.
- Remote lock/unlock: **NOT PHYSICALLY CERTIFIED, NOT IMPLEMENTED IN SOFTWARE EITHER.** No lock/unlock code exists in the backend, MQTT layer, or agent — the ten physical validation conditions in Master Requirements Part 11/38 remain entirely unaddressed. Do not represent lock/unlock as production-ready anywhere derived from this README.
- Keenon Cloud independence: **NOT ACHIEVED.** Path 1 still requires Keenon Cloud. Path 2 removes that dependency for telemetry, but has not been proven against a real robot.

## Project Structure

```
sakar robotics web/
├── docs/                     Approved documentation (copies; see docs/README.md for the full index)
│   ├── requirements/         Master requirements, companion requirements/roadmap docs, live API evidence
│   ├── security/             Standalone security requirements + risk register
│   ├── architecture/         System architecture + database specification
│   └── api/                  REST API specification
├── backend/                  Sakar Cloud backend (Java 21 + Spring Boot 4) — PHASE 1 FOUNDATION IMPLEMENTED, Phase 3 MQTT, live-broker-validated
├── web/                      Sakar web application (React + TypeScript) — PHASE 4 IMPLEMENTED against the real backend
├── mobile/                   Sakar mobile application (Flutter) — not yet implemented
├── robot/
│   └── SakarC40Agent/        Robot-resident Android agent — existing project, unmodified by Phase 1
├── database/                 Placeholder — the actual schema/migrations live in backend/src/main/resources/db/migration (Flyway)
├── infrastructure/           Docker/Nginx/deployment config — placeholder; backend/docker-compose.yml is the current dev environment
├── security/                 Security tooling/policy-as-code (distinct from docs/security/) — not yet implemented
├── tests/                    Placeholder — the actual automated tests live in backend/src/test/java (93 tests, see Testing Status)
└── README.md                 This file
```

## Applications

| Application | Location | Status |
|---|---|---|
| Sakar Cloud (backend) | `backend/` | **Phase 1 foundation implemented** — see Implementation Status |
| Web application | `web/` | **Phase 4 + Phase 6 implemented** — React + TypeScript admin UI against the real backend for auth/organizations/sites/robots/audit, and (Phase 6) Users/Roles/Tasks/Alerts/Cleaning history/non-lock Commands; (Eighth pass) Robot Detail → Map now renders the real, Sakar-backend-served map PNG (`GET /robots/{id}/map/image`) plus name/dimensions (`GET /robots/{id}/map`) — map points, area overlays, live position, and zoom/pan are not part of this slice yet; (Tenth pass) Robot Detail → Commands shows real per-command lifecycle history; (Eleventh pass) Robot Detail → Configuration is now a real Keenon sceneCode setup/view UI (`GET`/`PUT /robots/{id}/keenon/scene-config`) instead of a placeholder; (Twelfth pass) the Tasks panel's Create Task form now collects a real cleaning mode + area selection + optional repeat count for `CLEANING` tasks (previously sent `parameters: null`, which could never actually dispatch to a robot) — 5 of the original 7 Keenon-inspired tabs remain not connected; simulated data (clearly labeled) only for telemetry/events/errors/application logs/timeline, which still have no backend REST endpoint |
| Mobile application | `mobile/` | Not yet implemented |
| Robot Android tablet agent — Sakar Robot Agent (currently `SakarC40Agent`) | `robot/SakarC40Agent/` | Existing diagnostic app **now extended (Phase 3) with an MQTT telemetry/heartbeat link to the Sakar Cloud backend** (`api/` module); command-reception is still not implemented; not yet run against a real broker or physical robot |

**Core architecture rule:** the web and mobile applications communicate with Sakar Cloud only. Neither ever connects to a robot directly. The backend enforces this today for every implemented endpoint (Part 16); it cannot yet be exercised end-to-end because no web/mobile client or robot-agent connection exists yet.

## Implementation Status

Backend module status as implemented in `backend/src/main/java/com/sakarrobotics/cloud/`. **Foundation-only** means the schema/entity and, where noted, the read path exist, but full business workflows (dispatch, orchestration, ingestion) do not.

| Module | Status | Notes |
|---|---|---|
| Authentication (JWT, refresh-token rotation, login) | ✅ Complete | `auth/` — BCrypt, JWT access tokens, rotating/revocable refresh tokens, Redis-backed rate limiting, account lockout |
| RBAC | ✅ Complete | `iam/` — Part 19 role/permission matrix seeded via Flyway, `@PreAuthorize` enforced on every endpoint |
| Organization hierarchy | ✅ Complete | `org/` — Sakar root → distributor → sub-distributor → client → direct client, materialized-path tenant scoping |
| Sites | ✅ Complete | `org/Site*` — CRUD + organization-scoped authorization |
| Robot Registry | ✅ Complete | `robot/registry/` — register/activate/deactivate, tenant-scoped listing |
| Robot Models | ✅ Complete | `robot/registry/RobotModel*`, `RobotManufacturer*` |
| Robot Capabilities | ✅ Complete | `robot/registry/RobotCapability*` — per-model capability flags, `UNSUPPORTED_CAPABILITY` enforcement |
| Robot Adapter (abstraction) | ✅ Complete | `robot/adapter/` — interface + registry; `KeenonRobotAdapter` functional, `SakarRobotAdapter` intentionally a stub |
| Keenon Integration Foundation | ✅ Complete (read/control path) | `integration/keenon/` — OAuth token caching, area-id sync mapping, webhook intake with idempotency, manual on-demand cleaning-history sync trigger (`POST .../keenon/cleaning-history/sync`). Robot status now correctly calls the C-series `custom/clean/robot/status` endpoint (`scene/v1/robot/status` is T/W-series only — live-verified `610403` for this account's C40 S robots, see CURRENT PHASE Sixth pass). Map sync's `sceneCode` source (Sixth pass's open question) is resolved (Seventh pass): a robot's `sceneCode` is now Sakar-owned per-robot configuration (`KeenonRobotSceneConfig`, `PUT`/`GET .../keenon/scene-config`), with a new manual sync trigger `POST .../keenon/map/sync` — see `docs/KEENON_C40S_MAP_SCENE_INTEGRATION.md`. **KEENON-CLOUD DEPENDENT**, not a substitute for the local path |
| PostgreSQL | ✅ Complete (schema) | Full Part 13 schema via Flyway; production data-volume/performance not yet exercised |
| Flyway | ✅ Complete | 10 versioned migrations, `V1`–`V10` |
| Redis | ✅ Complete (foundation) | Login rate limiting and Keenon OAuth token caching implemented; not yet used for caching/pub-sub beyond that |
| Audit | ✅ Complete (foundation) | `audit/` — append-only `audit_logs`, recorded on login/logout; DB-role-level tamper hardening (`REVOKE UPDATE/DELETE`) not yet applied |
| OpenAPI / Swagger | ✅ Complete | `config/OpenApiConfig.java`, served at `/swagger-ui.html` |
| WebSocket Foundation | ✅ Complete (now publishing) | `websocket/` — STOMP auth on CONNECT, org-scoped SUBSCRIBE authorization, **plus (Phase 3) `RobotRealtimePublisher` actually publishing status/telemetry updates** |
| Docker (dev environment) | ✅ Complete | `backend/docker-compose.yml` — Postgres, Redis, Mosquitto, backend; **not** a production topology |
| MQTT (robot communication) | ✅ Complete (software, Phase 3) | `mqtt/` — full subscribe/ingest/ack pipeline (`MqttSubscriptionManager`, `MqttInboundListener`, `MqttInboundMessageService`); disabled by default (`SAKAR_MQTT_ENABLED`); **not run against a real broker or physical robot in this session** — broker-side auth/ACL/TLS remain a production requirement, see `docs/architecture/SAKAR_MQTT_ARCHITECTURE.md` §7 |
| Telemetry ingestion | ✅ Complete (software, Phase 3) | `telemetry/TelemetryIngestionService`/`HeartbeatService`/`RobotStatusService` — `robot_telemetry`/`robot_status` now have a real write path, driven by inbound MQTT |
| SRELS (events/errors/app logs) | 🔄 Partially Implemented (Phase 3) | `RobotEventIngestionService`/`RobotErrorIngestionService` write `robot_events`/`robot_errors` from MQTT; `application_logs` now receives MQTT lifecycle/ingestion events (`MqttLifecycleLogger`); no other module writes SRELS yet |
| User / Role administration API | ✅ Complete (Phase 6) | `iam/UserController`, `iam/RoleController`, `iam/UserService` — create/list/get/suspend/activate/change-role, gated by `USER_MANAGE`, org-hierarchy scoped; role list is read-only fixed reference data (`V9__seed_rbac.sql`) |
| Task orchestration | ✅ Complete (Phase 6 + Twelfth/Thirteenth passes) | `task/RobotTaskController`/`RobotTaskService` — create/start/pause/resume/stop/cancel with an explicit lifecycle-transition table and full event history. **(Twelfth pass)** Starting a `CLEANING` task now bridges into the existing `RobotCommandService.issue(...)`/`KeenonRobotAdapter` pipeline (no second Keenon/OAuth implementation) — a task only reaches `RUNNING` when the resulting `START_TASK` command is genuinely dispatched; an undispatched command moves the task to the (pre-existing, previously unused) `FAILED` status instead, never leaving it falsely `RUNNING`. **(Thirteenth pass)** Server-side duplicate-dispatch protection: the `START` transition now reads the task via a targeted pessimistic row lock (`RobotTaskRepository.lockByIdForUpdate`, `SELECT ... FOR UPDATE`, used only for `START` — no `@Version`/optimistic locking added elsewhere), so two concurrent `START` requests for the same task can never both dispatch a physical command; `areaIds` entries are also now validated as syntactically well-formed UUIDs before any `RobotCommand` is created or Keenon call attempted, closing an uncaught-`IllegalArgumentException`/500 gap. Every other transition (`PAUSE`/`RESUME`/`STOP`/`CANCEL`, and `START` for any non-`CLEANING` task type) remains Sakar-side bookkeeping only, unchanged |
| Alerting | ✅ Complete (Phase 6), two rule types only | `alert/` — real, evidence-driven low-battery (from live telemetry ingest) and offline (from a real `@Scheduled` sweep of `robot_status.last_seen_at`) rules only; idempotent (never duplicates an open alert); every other Master-Requirements alert type (critical error, task failure, emergency, comms loss, lock/unlock) has no generator yet and is not fabricated |
| Cleaning history (read path) | ✅ Complete (Phase 6) | `cleaning/CleaningController` — read-only; the table has exactly one writer, the task-completion hook in `RobotTaskService`, so a row always corresponds to a real completed `CLEANING`-type task, never a fabricated cleaning run |
| Remote Commands (non-lock) | ✅ Complete (Phase 6), agent now consumes + reports (Phase 7/8) | `command/RobotCommandController`/`RobotCommandService`/`CommandResultIngestionService`/`CommandExpiryService` — `START_TASK`/`STOP_TASK`/`PAUSE_TASK`/`RESUME_TASK`/`RETURN_TO_DOCK`/`GO_TO_POINT` only; **`LOCK`/`UNLOCK` are rejected by construction, no code path can issue either**; `SakarC40Agent` consumes the `commands` topic and reports lifecycle results, so `RobotCommand.status` genuinely advances past `SENT`. Three agent-side executors exist: `START_TASK` → `SimulatedRobotCommandExecutor` (software placeholder, no Peanut SDK call, no physical robot contact — no cleaning API exists to call); `RETURN_TO_DOCK` → `PeanutSdkReturnToDockExecutor` (**real** Peanut SDK `BatteryComponent.autoCharge()` call, gated by `OperatingMode.HARDWARE_TEST`, reports the honest `COMMAND_DISPATCHED` status rather than a fabricated `COMMAND_SUCCESS` — CODE VERIFIED + SOFTWARE TEST VERIFIED, PHYSICAL ROBOT VALIDATION: NOT PERFORMED); `GO_TO_POINT` → `PeanutSdkGoToPointExecutor` (**real** Peanut SDK `NavigationComponent.setTarget(IDataCallback, int)` call with a caller-supplied, never-invented, non-negative `destinationId`, same gating and same `COMMAND_DISPATCHED` honesty — see `C40_S_GO_TO_POINT_SDK_INVESTIGATION.md`). `STOP_TASK`/`PAUSE_TASK`/`RESUME_TASK` still have no agent-side executor at all. (Ninth pass) `GET /commands/{commandId}/results` now exposes the `command_results` lifecycle history for one command, paginated, newest-first. See Current Phase note above |
| Command signing/dispatch (production-grade) | ⏳ Planned | Nonce + expiry exist on `RobotCommand`, expiry is now actively enforced (`CommandExpiryService`); no cryptographic signature yet — the agent-side consumer now exists (Phase 7) but its only executor is a software simulation, so this is still short of the full Phase 6/7 roadmap spec |
| Robot lock/unlock (software) | ⏳ Planned | `lock/` schema only; no endpoint exists to trigger it, and `RobotCommandController` explicitly cannot be used for it (see Remote Commands row above) — still excluded per Master Requirements Part 11/38 |
| Analytics | ⏳ Planned | No code exists yet |
| Open Platform (proposed product expansion) | 📋 Requirements only, NOT implemented | `docs/requirements/SAKAR_PLATFORM_KEENON_PARITY_REQUIREMENTS.md` Module A — no entity, controller, or migration exists |
| OTA / Deployment (proposed product expansion) | 📋 Requirements only, NOT implemented | Same document, Module B — explicitly does not assume Keenon firmware push is possible |
| IoT / Elevator Integration (proposed product expansion) | 📋 Requirements only, NOT implemented | Same document, Module C — elevator protocol is an open question, not assumed |
| SakarC40Agent integration | 🔄 Partially Implemented (Phase 3 + 7/8) | MQTT telemetry/heartbeat/events/errors link exists (`:api` module) and is wired into `SakarC40Application`; Phase 7/8 adds inbound command consumption (`CommandDispatcher`, routed by `CompositeRobotCommandExecutor`) and lifecycle result reporting for three command types: `START_TASK` (`SimulatedRobotCommandExecutor`, a software placeholder, not a Peanut SDK call), `RETURN_TO_DOCK` (`PeanutSdkReturnToDockExecutor`, a REAL `BatteryComponent.autoCharge()` call, gated by `OperatingMode.HARDWARE_TEST`), and `GO_TO_POINT` (`PeanutSdkGoToPointExecutor`, a REAL `NavigationComponent.setTarget(IDataCallback, int)` call with a caller-supplied destination id, same gating); `STOP_TASK`/`PAUSE_TASK`/`RESUME_TASK` have no executor at all yet; not yet run against a real broker or physical robot — see Robot Integration Status |
| Robot MQTT credential issuance | ✅ Complete (Phase 3) | `RobotCredentialService`, `POST /api/v1/robots/{id}/mqtt-credentials` — `robot_credentials` now populated; broker does not yet enforce it |
| Physical C40 validation | ⚠️ Requires Physical Test | Not performed; gated by Master Requirements Part 38 |
| Remote lock/unlock certification | ⚠️ Requires Physical Test | Not performed; not even software-implemented yet; gated by Master Requirements Part 11/38 |

## Database Status

The PostgreSQL + Flyway foundation is implemented: 11 migrations (`backend/src/main/resources/db/migration/V1__core_and_iam.sql` through `V11__mqtt_inbound_messages.sql`) create the full schema and seed the RBAC matrix plus Keenon C40 S / Sakar CleanBot 5000 Plus reference data. Current tables:

`organizations`, `sites`, `roles`, `permissions`, `role_permissions`, `users`, `refresh_tokens`, `robot_manufacturers`, `robot_models`, `robot_capabilities`, `robots`, `robot_credentials`, `robot_status`, `robot_telemetry`, `robot_events`, `robot_errors`, `robot_alerts`, `application_logs`, `robot_commands`, `command_results`, `robot_locks`, `robot_tasks`, `task_events`, `cleaning_sessions`, `charging_sessions`, `maps`, `map_points`, `notifications`, `audit_logs`, `keenon_area_mappings`, `vendor_webhook_events`, `mqtt_inbound_messages`, `keenon_robot_scene_configs` (Seventh pass — Sakar-owned per-robot Keenon `sceneCode` configuration, migration `V16`).

The migration count/list above (`V1`–`V11`) is itself stale pre-existing documentation debt (the repository actually has migrations through `V16` — see the Sixth pass note on undocumented existing Keenon work); reconciling the full migration history is out of scope for this pass, which only adds `V16__keenon_robot_scene_config.sql`.

**Schema existing does not mean the corresponding workflow is complete.** As of Phase 3, `robot_status`, `robot_telemetry`, `robot_events`, `robot_errors`, `robot_credentials`, and `mqtt_inbound_messages` have real application code populating them (via the MQTT ingestion pipeline). As of Phase 6, `robot_commands`, `robot_tasks`, `task_events`, `cleaning_sessions`, `robot_alerts`, and `users`/`roles` (write paths, not just seed data) also have real application code populating them. `robot_locks` still has no application code populating it at all — see Implementation Status above.

## Testing Status

**102 / 102 automated backend tests passing** (`cd backend && ./mvnw clean test`) — 93 pre-existing (32 foundation + 27 Phase 3 + 12 Phase 3 Security Hardening + 22 Phase 6) + 9 Phase 7 tests. Pre-existing coverage includes login/lockout/rate-limiting, refresh-token rotation and reuse detection, RBAC enforcement, tenant isolation, organization-hierarchy logic, robot capability gating, Keenon adapter response mapping, webhook idempotency, audit-log writes, JWT expiry/tamper handling, MQTT topic build/parse, full ingestion-pipeline acceptance/rejection paths, heartbeat/telemetry/event/error/presence ingestion, ACK generation, robot MQTT credential provisioning/rotation, negative-sequence/oversized-payload/rate-limit rejection, credential revocation/audit-trail verification, WebSocket tenant isolation, user/role/task/alert/cleaning/command administration (Phase 6). Phase 7 coverage (`CommandResultIngestionServiceTest`, 6 tests): full RECEIVED→EXECUTING→COMPLETED lifecycle advancing `RobotCommand.status` and appending `CommandResult` history rows, a terminal status never being resurrected by a late/out-of-order report, an unknown `commandId` being ignored without error, a result claiming a command that belongs to a *different* robot being rejected (tenant/robot-spoofing check independent of the outer envelope's own robotId check), an unrecognized status string being ignored without error, and — added this second pass — `RETURN_TO_DOCK`'s `DISPATCHED` report mapping to `COMMAND_DISPATCHED` (not `COMMAND_SUCCESS`), confirmed terminal/frozen, with its `CommandResult` and `audit_logs` rows both verified. Phase 7 coverage (`CommandExpiryServiceTest`, 3 tests): an overdue non-terminal command is marked `COMMAND_TIMEOUT` with a `CommandResult` row, a not-yet-expired command is left alone, and an already-terminal command is never touched even past its expiry. See `docs/security/SAKAR_PHASE_3_SECURITY_HARDENING_REPORT.md` for the Phase 3 finding-by-finding account.

**28 / 28 automated frontend tests passing** (`cd web && npm run test -- --run`) — 21 pre-existing (login, protected routes, JWT decode, robots list) + 7 new this pass: `UsersPage.test.tsx` (real GET /users rendering, error state, POST /users create-flow with the real request shape, POST /users/{id}/suspend) and `AlertsPage.test.tsx` (real GET /alerts rendering using the real OPEN/ACKNOWLEDGED/RESOLVED status vocabulary — not the old simulated ACTIVE vocabulary, error state, POST /alerts/{id}/acknowledge). `npm run build` (`tsc -b && vite build`) succeeds; `npm run lint` (oxlint) is clean (only pre-existing warnings in files untouched this pass). **Not performed:** a live browser session against a real running backend (no Docker/Postgres/Redis available in this environment) — see the Current Phase note above.

**37 / 37 automated Robot Agent (`:api` module) tests passing** (`cd robot/SakarC40Agent && ./gradlew :api:test`) — 18 pre-existing (topic building, Gson↔Jackson wire-format compatibility, the bounded offline queue, safe no-broker behavior, both schedulers, and TLS (`ssl://`) configuration safety) + 10 Phase 7 `START_TASK` tests (`CommandDispatcherTest`, `CommandPayloadSerializationTest`, one `AgentMqttTopicsTest` case) + 9 Phase 7 `RETURN_TO_DOCK` tests (second pass): `PeanutSdkReturnToDockExecutorTest` (3 — reports EXECUTING before calling the gateway, gateway acceptance reports `DISPATCHED` and never `COMPLETED`, gateway error reports `FAILED`), `CompositeRobotCommandExecutorTest` (4 — routes to the right executor by `commandType`, an unregistered type reports FAILED without throwing, a `null` `commandType` reports FAILED without throwing — this caught a real `Map.of(...).get(null)` `NullPointerException` bug, since fixed), and two new `CommandDispatcherTest` cases (the dispatcher's own `reportDispatched` path, and a full integration test wiring the *real* `PeanutSdkReturnToDockExecutor` with only the SDK boundary faked). `:app:compileDebugJavaWithJavac` (where `SimulatedRobotCommandExecutor`, `RealReturnToDockGateway`, and `SakarC40Application` live, alongside the real `:robot`/`:charging`/`:sdk` wiring for `RETURN_TO_DOCK`) also builds successfully against the real local Android SDK.

Build/package: `./mvnw clean test` and `./mvnw package -DskipTests` both succeed, producing `backend/target/sakar-cloud-backend-0.0.1-SNAPSHOT.jar`.

No coverage percentage is reported here because none has been measured (no coverage tool is wired into the build) — do not infer one. **None of the above tests exercise a running MQTT broker or a physical robot** — see `docs/requirements/SAKAR_PHASE_3_IMPLEMENTATION_REPORT.md` §9 "Known Limitations".

## Security Status

Implemented and verified by the test suite above:

| Control | Status |
|---|---|
| Password hashing (BCrypt) | ✅ Complete |
| JWT access tokens | ✅ Complete |
| Refresh-token handling (rotation, reuse detection, revocation) | ✅ Complete |
| RBAC (method-level `@PreAuthorize`) | ✅ Complete |
| Tenant authorization (organization-hierarchy scoping) | ✅ Complete |
| IDOR/BOLA protection | ✅ Complete for robot lookups (tested); not yet exercised for every future resource type |
| Audit foundation | ✅ Complete (foundation) — append-only writes; DB-role `REVOKE UPDATE/DELETE` hardening not applied |
| Login rate limiting | ✅ Complete (Redis-backed) |
| Account lockout | ✅ Complete |
| Secret protection | ✅ Complete for what exists — Keenon credentials and (Phase 3) MQTT credentials env-var/git-ignored-file-only, never in source; secrets-manager integration for production deployment not yet built |
| WebSocket authorization | ✅ Complete, now with real traffic — CONNECT auth + org-scoped SUBSCRIBE (unchanged); `RobotRealtimePublisher` (Phase 3) is the first real event traffic exercising it |
| MQTT robot identity + tenant verification (software) | ✅ Complete (Phase 3) — `MqttInboundMessageService` cross-checks robot/org/site against the registered `Robot` row independent of transport-level auth |
| MQTT broker-side authentication/ACL/TLS | ❌ Not implemented — dev broker allows anonymous, plaintext connections; **production requirement**, see `docs/architecture/SAKAR_MQTT_ARCHITECTURE.md` §7 |
| MQTT client-side TLS configuration | ✅ Complete (Security Hardening) — `ssl://` supported end-to-end with real CA validation, optional private-CA truststore; never run against a live TLS broker |
| MQTT message idempotency/replay handling | ✅ Complete (Phase 3) — `mqtt_inbound_messages` unique constraint |
| MQTT payload size limit + per-robot rate limiting | ✅ Complete (Security Hardening) — 64 KiB cap enforced before parsing; Redis-backed 120 msg/60s per-robot limit |
| Robot MQTT credential revocation + audit logging | ✅ Complete (Security Hardening) — `DELETE /api/v1/robots/{id}/mqtt-credentials`; provision/rotate/revoke all audit-logged, secret never logged |
| WebSocket tenant-isolation test coverage | ✅ Complete (Security Hardening) — previously untested; new unit-test suite covers cross-org/own-org/unrecognized-destination/user-queue cases |

Explicitly incomplete: command signing/replay protection (no commands are dispatched yet), message signing independent of TLS (documented, not implemented — see the hardening report §3.5), Android/agent-side security beyond MQTT credential wiring (Part 21 — kiosk/device-owner mode, Keystore-backed credential storage, secure updates — all still untouched/undone, see hardening report §9), MQTT broker security (TLS termination, per-robot credential enforcement, topic ACLs — client-side wiring exists, not enforced against a real broker), DB-role-level audit tamper hardening, and every physical-robot-dependent control in Part 11/38.

## Security

`docs/security/SAKAR_SECURITY_REQUIREMENTS.md` remains the authoritative control specification. The table above documents what Phase 1 + Phase 3 actually implement against it — this repository is no longer purely "specified, not implemented" for the backend or the agent's communication layer, but most of Parts 16–29 still describe target state beyond what's implemented (MQTT broker hardening against a real deployment, Android/agent security beyond credential wiring, backup/DR, monitoring, security testing). The platform's highest-risk feature — remote motor lock/unlock — remains **not production-ready and not software-implemented at all**; Phase 3 changes nothing about the physical validation requirement (Part 11/38) and deliberately does not touch lock/unlock.

## Documentation

All approved specifications live under `docs/` — see `docs/README.md` for the full index. `docs/requirements/SAKAR_ROBOT_PLATFORM_MASTER_REQUIREMENTS.md` is the authoritative requirements reference this README is checked against; where this README and that document diverge, the master requirements document governs and this README should be corrected.

## Proposed Product Expansion (Keenon Parity) — NOT IMPLEMENTED

`docs/requirements/SAKAR_PLATFORM_KEENON_PARITY_REQUIREMENTS.md` scopes three product modules observed in Keenon Cloud (the vendor's own dashboard) that Sakar's platform has no equivalent of today: **Open Platform** (third-party API client/key management), **OTA / Deployment** (firmware/app version tracking and rollout), and **IoT / Elevator Integration** (non-robot device registration and elevator-call authorization). This is a **requirements/scope document only** — no entity, controller, migration, or UI for any of the three exists in this repository, and none should be inferred from the Keenon reference screenshots that motivated scoping them. If approved, they would become Roadmap Phases 11-13, sequenced after and independent of the approved Phase 0-10 work; see that document's §18 for the recommended order and §17 for explicit out-of-scope items (never copy Keenon branding, never invent a firmware-push or elevator protocol that hasn't been vendor-confirmed).

## External Peanut SDK

The Keenon **Peanut SDK** (`peanut-sdk-v1.3.0`, including the vendor's sample app and the decompiled technical study material) is an **external, read-only vendor dependency** that lives at `D:\Sakar Robotics Projects\peanut-sdk-v1.3.0`, outside this project workspace. It has not been moved, copied, or modified, and Phase 1 backend work did not touch it. `robot/SakarC40Agent/sdk/libs/peanut-sdk-release.aar` is the pre-existing vendored binary that `SakarC40Agent` already depends on, unmodified, and excluded from Git tracking (see `.gitignore`).

## Roadmap

**Completed this pass (Phase 6 — Fleet Operations, approved scope):** Users/Roles administration REST API, task orchestration lifecycle, low-battery/offline alerting, cleaning-history read path, and non-lock remote commands, all built by reusing the existing `TenantAccessGuard`/`AuditService`/`SakarErrorCode`/`RobotCapabilityService` chokepoints rather than introducing parallel ones — see Implementation Status above and `docs/requirements/SAKAR_ROBOT_PLATFORM_ROADMAP.md`'s Phase 6 entry. 22 new backend tests added, 93/93 passing.

**Also completed this pass (Decision 2 — Keenon-parity scope-only work, explicitly NOT implemented):** `docs/requirements/SAKAR_PLATFORM_KEENON_PARITY_REQUIREMENTS.md` scopes three proposed product-expansion modules (Open Platform, OTA/Deployment, IoT/Elevator Integration) surfaced by Keenon Cloud reference screenshots, and the roadmap's "Proposed Product-Expansion Phases" section (11-13) records where they would sit **if** separately approved. No backend code for any of the three exists — see that document's status banner and §17 "Out-of-Scope Items."

**Also completed this pass (Phase 6 frontend integration):** the web application was rewired from `web/src/mocks/simulated.ts` onto the real Phase 6 endpoints — Users (`UsersPage`, including create/suspend/activate/role-change), Roles (`RolesPage`, real GET /roles), Tasks (`TasksPage` + a shared `RobotTasksPanel` reused on the Robot Detail page, including lifecycle start/pause/resume/stop/cancel), Alerts (`AlertsPage` + `RobotAlertsPanel`, including acknowledge/resolve), Cleaning history (new `CleaningPage` + `RobotCleaningPanel`), and a new Robot Detail "Commands" tab (`CommandsPanel`) for non-lock remote commands only — LOCK/UNLOCK remain unofferable by construction. `generateAlerts`/`generateTasks`/`generateUsers`/`generateRoles` were removed from the mocks file entirely; Telemetry/Events/Errors/Application Logs/Timeline remain simulated (no backend endpoint exists for any of them). The Dashboard's Locked/Faulted/Cleaning/Charging fleet metrics are now honestly marked Unavailable rather than fabricated; Low Battery is now real (derived from open `LOW_BATTERY` alerts). 7 new frontend tests added (28/28 passing); full live-browser verification against a running backend was not performed (no Docker/Postgres/Redis in this environment).

**Prior pass: Story 3 — Implement SakarC40Agent Communication Layer (software).** The MQTT-based telemetry channel between Sakar Cloud and `SakarC40Agent` exists and is automated-test-verified, with `robot_status`/`robot_telemetry`/`robot_events`/`robot_errors` ingestion wired. Still not done: replacing the `SakarRobotAdapter` stub with a working local-path *command* adapter, and any physical/network validation.

**This pass (Phase 7 — Robot Agent Command Loop, branch `feature/robot-agent-command-loop`):** investigated exactly how the Peanut Clean app and the officially-distributed Peanut SDK implement robot operations (independent decompilation of both, cross-verified against each other — see `ROBOT_AGENT_COMMAND_LOOP_INVESTIGATION_AND_DESIGN.md`), confirmed the Peanut SDK AAR `SakarC40Agent` bundles has no cleaning-control API, and — rather than reverse-engineering Peanut Clean's undocumented internal CoAP endpoints without a business decision to do so — built the full software command loop with an explicitly-labeled simulation standing in for real execution: `SakarC40Agent` now consumes commands (`CommandDispatcher`) and reports lifecycle results over the existing EVENT channel; the backend now applies those results idempotently (`CommandResultIngestionService`) and resolves commands the agent never answers (`CommandExpiryService`). 8 new backend tests (101/101) and 10 new/extended agent tests (28/28), all passing; no MQTT broker or physical robot used.

**Second pass, same branch (`RETURN_TO_DOCK`):** re-verified (fresh `javap` decompilation) that `BatteryComponent.autoCharge()` — unlike cleaning — genuinely exists in the officially-distributed SDK, and wired a REAL executor for it (`PeanutSdkReturnToDockExecutor` → `C40RobotController.returnToDock()`, gated by the pre-existing `OperatingMode.HARDWARE_TEST` guard, never bypassed) alongside the unchanged `START_TASK` simulation. Because SDK acceptance isn't confirmed physical completion, added an honest new terminal status (`COMMAND_DISPATCHED`) to the existing lifecycle rather than fabricating success — no new MQTT topic/message type, no DB migration. 1 new backend test (102/102) and 9 new agent tests (37/37), all passing; caught and fixed a real `NullPointerException` (`Map.of(...).get(null)`) via test coverage. `START_CLEANING`'s status is unchanged — still not implemented, per instruction.

**Next development step:** either (a) obtain Keenon's cooperation/an official SDK extension for cleaning control, or (b) make an explicit, separately-reviewed business decision on whether to call Peanut Clean's undocumented internal CoAP endpoints, before replacing `SimulatedRobotCommandExecutor` with anything real — see `ROBOT_AGENT_COMMAND_LOOP_INVESTIGATION_AND_DESIGN.md` §7/§11/§22 for the full analysis. For `GO_TO_POINT` specifically: enumerate and confirm real destination ids against a specific C40's currently-loaded map (`NavigationComponent.getAllDestPose()`, already present in the bundled AAR, not yet wired into this project) before this command type can be used for anything beyond software testing — see `C40_S_GO_TO_POINT_SDK_INVESTIGATION.md` §13/§14. Separately: live-browser verification of the frontend integration pass against a real running backend once Postgres/Redis/Docker are available; Phase 0 physical validation (Master Requirements Part 38) and a live network test of the MQTT pipeline (including `RETURN_TO_DOCK`'s and `GO_TO_POINT`'s real SDK calls, under supervised `HARDWARE_TEST` conditions) remain outstanding and continue to gate remote lock/unlock and production sign-off for any physical-action command.

## Team Ownership

**Uday:**
- Robot platform
- Robot adapter
- Keenon integration
- Robot tasks/cleaning
- Robot commands

**Riyaz:**
- Backend core
- Authentication
- RBAC
- Organization
- Site
- API security

**Rohit:**
- Database
- Telemetry
- SRELS
- Audit
- Testing
- Infrastructure

## Development Rules

- **`README.md` must be updated whenever a major feature/story/phase changes project status.** A stale README is treated as a defect — verify actual repository state (`git branch --show-current`, `git log -1 --oneline`, `git status --short`, the test suite) before editing it, rather than copying forward prior claims.
- Do not mark a capability `✅ Complete` unless the code implementing it exists, compiles, and is covered by a passing test. Use `🔄 In Progress`, `⏳ Planned`, or `⚠️ Requires Physical Test`/`Requires Vendor Support` otherwise.
- Never claim physical robot control, production MQTT, production lock/unlock, complete `SakarC40Agent` integration, or Keenon Cloud independence unless actually implemented and verified — see Master Requirements Part 40's "API accepted ≠ robot executed ≠ verified" rule, which applies equally to claims made in this README.

## Legacy documents at the project-collection level

`D:\Sakar Robotics Projects\` (one level above this workspace) also contains `Sakar_Robotics_SRS.docx`/`.pdf`, a pre-existing document that predates this workspace. It was left in place, untouched, and is not part of this workspace's `docs/` index.

---

**Current authorized scope:** backend Phase 1 is complete; Story 3 (`SakarC40Agent` MQTT communication layer), Phase 6 (Users/Roles, Tasks, Alerts, Cleaning history, non-lock Remote Commands), and Phase 7/8 (Robot Agent Command Loop — agent-side command consumption and result reporting for `START_TASK` (software simulation), `RETURN_TO_DOCK`, and `GO_TO_POINT` (both real Peanut SDK calls, `OperatingMode.HARDWARE_TEST`-gated)) are implemented in software and test-verified, per the roadmap above, but **uncommitted** on `feature/robot-agent-command-loop` (branched off the already-committed `feature/phase6-fleet-operations`) — pending review/approval before any commit, merge, or push. Open Platform / OTA / IoT-Elevator Integration are requirements-only (see Proposed Product Expansion above) and remain explicitly not implemented pending separate review. Physical C40 connection, physical robot control (including `RETURN_TO_DOCK`'s and `GO_TO_POINT`'s now-real SDK calls), and remote lock/unlock certification remain out of scope until the separate physical-validation go-ahead described in Master Requirements Part 33/38 is given, and until a business decision is made on how `SimulatedRobotCommandExecutor`'s remaining command types (`START_TASK`, and any future cleaning-control commands) should eventually be replaced (see Roadmap above). `GO_TO_POINT` additionally requires enumerating and confirming real destination ids against a specific robot's map before any physical use — see `C40_S_GO_TO_POINT_SDK_INVESTIGATION.md`.
