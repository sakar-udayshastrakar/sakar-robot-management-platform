# security/

**Status: NOT YET IMPLEMENTED.** This directory is distinct from `docs/security/` — `docs/security/` holds the *specification* (what the security controls must be); this directory is reserved for actual security *tooling and policy artifacts* once implementation begins: CI/CD security-scanning configuration (dependency vulnerability scanning per `docs/security/SAKAR_SECURITY_REQUIREMENTS.md` Section 16), secret-scanning configuration, and any security-policy-as-code files.

**Nothing in this directory today performs any security function.** All current security work is documentation: `docs/security/SAKAR_SECURITY_REQUIREMENTS.md` and `docs/security/SAKAR_SECURITY_RISK_REGISTER.md`.

**Current phase: DOCUMENTATION / ARCHITECTURE PREPARATION.**
