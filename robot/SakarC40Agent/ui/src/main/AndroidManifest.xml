<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          No longer the app's launcher: SakarOperatorActivity (:operator-ui) is now the
          Sakar CleanBot launcher. This diagnostic-only screen is kept, unmodified, as an
          engineering/debugging entry point - reachable from Super User > System Settings >
          "Open Legacy Diagnostics" (explicit intent), not from the home screen launcher.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />
    </application>

</manifest>
