package com.sakarrobotics.cloud.robot.registry;

/** SAKAR_ROBOT_PLATFORM_ARCHITECTURE.md §7 — current vs. target integration path, per robot model. */
public enum IntegrationPath {
    KEENON_CLOUD_DEPENDENT,
    SAKAR_OWNED_LOCAL
}
