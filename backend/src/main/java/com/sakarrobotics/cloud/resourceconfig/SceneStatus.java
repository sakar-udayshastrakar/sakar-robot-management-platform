package com.sakarrobotics.cloud.resourceconfig;

/** Shared by {@link ResourceScene} and {@link MarketingMaterial} — a draft is not yet visible/effective, published is live. */
public enum SceneStatus {
    DRAFT,
    PUBLISHED
}
