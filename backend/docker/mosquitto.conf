# Local development MQTT broker only. Production requires TLS, per-robot
# credentials, and topic ACLs (Master Requirements Part 23) — none of that
# is configured here; this file exists only so `docker-compose up` gives
# developers a broker to point SAKAR_MQTT_BROKER_URL at locally.

listener 1883
allow_anonymous true
persistence false
